# DevTools Exploration Report

**Student Name:** Nahashon Mutahi
**Date:** June 21, 2026

## What HTML tags are used on the page?

The page uses several basic HTML tags, including:

* `<html>`
* `<head>`
* `<title>`
* `<style>`
* `<body>`
* `<div>`
* `<h1>`
* `<p>`
* `<a>`

## What is the page title?

**Example Domain**

## How many headings are there?

There is **1 heading** on the page:

* `<h1>Example Domain</h1>`



The navigation menu is wrapped inside the **`<nav>`** tag. This tag is used to contain navigation links and menus.

## How is the search bar structured?

The search feature is structured using:

* A `<form>` element
* An `<input type="search">` field
* Supporting buttons and labels for accessibility

 

## What happens when you hover over links?

When hovering over links:

* The cursor changes to indicate the element is clickable.
* CSS `:hover` styles are applied.
* Links may change color, background color, or underline depending on the site's styling rules.

--


## Identify 5 different HTML elements

The following HTML elements were identified:

1. `<header>`
2. `<div>`
3. `<form>`
4. `<input>`
5. `<footer>`

## Find a form element and list its inputs

Wikipedia's search form contains:

* Search text input (`type="search"` or `type="text"`)
* Submit button (`type="submit"`)

    
